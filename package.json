{
  "name": "zeromq-ng",
  "version": "5.0.0-beta.1",
  "description": "Next-generation ZeroMQ bindings for Node.js",
  "main": "lib/index",
  "gypfile": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/rolftimmermans/zeromq-ng.git"
  },
  "dependencies": {
    "detect-libc": ">= 0.2.0",
    "node-addon-api": "rolftimmermans/node-addon-api#master",
    "node-gyp": ">= 3.6.2",
    "prebuild-install": ">= 2.2.2"
  },
  "devDependencies": {
    "benchmark": ">= 0",
    "chai": ">= 4.1",
    "eslint": ">= 4.0",
    "mocha": ">= 4.0",
    "prebuild": ">= 6.2.1",
    "semver": ">= 0",
    "should": ">= 0",
    "weak": ">= 1.0"
  },
  "engines": {
    "node": ">= 8.6"
  },
  "scripts": {
    "install": "detect-libc prebuild-install || script/build.sh",
    "prebuild": "detect-libc prebuild --verbose --target 8.6.0 --strip --upload",
    "test": "eslint lib && script/fmt.sh && script/build.sh --debug --verbose && ZMQ_BUILD=Debug mocha",
    "bench": "script/build.sh --verbose && node --use_strict --harmony_async_iteration --expose-gc test/bench/send-only.js"
  },
  "keywords": [
    "zeromq",
    "zmq",
    "0mq",
    "ømq",
    "libzmq",
    "native",
    "binding",
    "addon"
  ],
  "license": "MIT"
}
